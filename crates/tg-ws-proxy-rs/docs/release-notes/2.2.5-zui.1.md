# v2.2.5-zui.1

## Flowseal 1.10 parity

- Cap reassembled incoming WebSocket messages at 16 MiB, matching the limit added by `Flowseal/tg-ws-proxy` 1.10.0 while retaining tungstenite's built-in fragmented-message reassembly.
- Make the upstream integration tests deterministic on Windows: accept both expected direct-DC attempts in the fake proxy and skip only the two subdomain-localhost DNS cases unsupported by the Windows resolver.
